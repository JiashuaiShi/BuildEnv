# Docker容器开发环境配置
# 基本容器设置
BENCH_CONTAINER_NAME=shuai-bench-test
BENCH_IMAGE_NAME=shuai/bench-test:20250506

# 网络设置
BENCH_SSH_PORT=28970

# 用户配置
USER_NAME=shijiashuai
USER_ID=2034
GROUP_NAME=lush-dev
GROUP_ID=2000
USER_PASSWORD=phoenix2024

# 环境变量设置
LANG=zh_CN.UTF-8
TERM=xterm-256color
CONDA_DISABLE_PROGRESS_BARS=1

# 容器特权设置
# 这些设置允许容器在宿主机上拥有更多权限，适用于开发环境
# 注意：在生产环境中应谨慎使用这些设置
PRIVILEGED=true
NETWORK_MODE=host
PID_MODE=host
IPC_MODE=host

# 共享内存设置
SHM_SIZE=16g

# 代理设置（需要时取消注释）
# HTTP_PROXY=http://proxy:port
# HTTPS_PROXY=http://proxy:port
# NO_PROXY=localhost,127.0.0.1

# 挂载卷设置
# 这些将在docker-compose.yaml中引用
VOLUMES_CONFIG=/var/run/docker.sock:/var/run/docker.sock,/data:/data,/data1:/data1,/data2:/data2,/mnt/nas1:/mnt/nas1,/data_melody:/data_melody,/data-melody:/data-melody,/lush_test:/lush_test,/data-lush:/data-lush
