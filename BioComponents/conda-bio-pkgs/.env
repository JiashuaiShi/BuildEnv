# BioComponents/conda-bio-pkgs/.env
# Configuration for the conda-bio-pkgs toolkit image

# Image repository and tag
BIO_CONDABIOPKGS_IMAGE_REPO=shuai/conda-bio-pkgs
BIO_CONDABIOPKGS_IMAGE_TAG=latest # Or a specific version/date tag

# Container name (if run via compose for specific tasks, can be simpler)
BIO_CONDABIOPKGS_CONTAINER_NAME=conda-bio-pkgs-task-runner

# Locale and Terminal (can be useful even for toolkit images if they output text)
LANG=en_US.UTF-8
TERM=xterm-256color