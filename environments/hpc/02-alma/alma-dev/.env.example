# --- 用户凭证配置 ---
# 开发用户名 (与 Dockerfile 中的 ARG 保持一致)
DEV_USER=shijiashuai

# 开发用户密码 (请务必修改为强密码)
DEV_PASSWORD=change_this_password

# --- 用户与用户组 ID ---
# 用于确保容器内外的文件权限一致
# 请根据您宿主机的用户ID进行调整，使用 `id -u` 和 `id -g` 命令查看
USER_UID=2034
GROUP_GID=2000
