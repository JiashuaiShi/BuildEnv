# Environment Configuration for dev-cpp-python

# --- Image Naming ---
APP_IMAGE_REPO=lessup/dev-cpp-python
APP_IMAGE_TAG=latest

# --- Container Naming ---
APP_CONTAINER_NAME=dev-cpp-python-container
APP_SERVICE_NAME=dev-cpp-python-service

# --- Dependency Chain ---
# Specifies the variant this app is based on
VARIANT_IMAGE_NAME=ubuntu-supervisor
# Specifies the base the variant is based on
BASE_IMAGE_NAME=ubuntu

# --- Development User ---
# User for exec sessions
DEV_USER_NAME=devuser

# --- Proxy Settings (if needed) ---
# Example: http://host.docker.internal:7890
HTTP_PROXY=
HTTPS_PROXY=
